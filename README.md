# Alcidion workspace

Two independent git repos:
- `backend/` — ASP.NET Core 9 Web API (deploy target: SmarterASP.NET)
- `frontend/` — React 19 + Vite

See `notes.md` for the plan, and `docs/lifecycle-and-hooks.md` for how a request travels through the
API and how a session is recorded in the UI.
